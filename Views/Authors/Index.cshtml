@model IEnumerable<Library_MVC.Models.Author>

@{
    ViewBag.Title = "Index";
}

<h2>Authors</h2>

<p>
    @if (User.IsInRole("Administrator") || User.IsInRole("Librarian"))
    {

        @Html.ActionLink("Create New", "Create")

    }
</p>
<table class="table" id="authorsTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>

                    @Html.ActionLink("Details", "Details", new { id = item.AuthorId })|

                    @if (User.IsInRole("Administrator") || User.IsInRole("Librarian"))
                    {
                        @Html.ActionLink("Edit", "Edit", new { id = item.AuthorId })
                    }|


                    @if (User.IsInRole("Administrator") || User.IsInRole("Librarian"))
                    {
                        <button class="btn-link js-delete" data-id="@item.AuthorId">Delete</button>
                    }

                </td>
            </tr>
        }
    </tbody>
</table>

@section scripts{
    <script>
        var table = $("#authorsTable").DataTable();  //ovaa linija ni e potrebna za JqueryDataTables

        // ova ni e potrebno za brishenje so AJAX
        $("#authorsTable .js-delete").click(function () {
            var button = $(this);                             // referenca /cel objekt ako e so $, do selektiranoto kopche za brishenje,kade kako atrib. go imame id-to na filmot
            bootbox.confirm("Are you sure you want to delete the author?",
                function (result) {
                    if (result) {
                        $.ajax({
                            method: "GET", //"DELETE",
                            url: "/Authors/Delete/" + button.attr("data-id"),
                            success: function (result) {
                                table.row(button.parents("tr")).remove().draw();
                            }
                        });
                    }

                });
        });

    </script>

}

